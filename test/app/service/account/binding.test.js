'use strict';
const { app, assert } = require('egg-mock/bootstrap');

describe('create()', () => {
  /* it('should create account binding', async () => {
    const ctx = app.mockContext();
    const result = await ctx.service.account.binding.create({
      uid: 1042,
      account: 'test3',
      platform: 'test3',
    });
    assert(result);
  });
  it('should get account binding', async () => {
    const ctx = app.mockContext();
    const result = await ctx.service.account.binding.get(1042, 'test');
    assert(result.id === 23);
  });
  it('should change main account', async () => {
    const ctx = app.mockContext();
    const result = await ctx.service.account.binding.updateMain(1042, 'test');
    assert(result);
  }); */
});
